import React from "react";
import { Text, View, Dimensions } from "react-native";

const Note = ({ item }) => {
  return (
    <View>
      <Text>Note Detail</Text>
    </View>
  );
};

export default Note;
